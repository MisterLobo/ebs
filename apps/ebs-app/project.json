{
  "name": "ebs-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/ebs-app/",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/ebs-app/",
        "command": "next dev --turbopack"
      }
    },
    "dev:tls": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/ebs-app/",
        "command": "next dev --turbopack --experimental-https"
      }
    }
  },
  "tags": []
}